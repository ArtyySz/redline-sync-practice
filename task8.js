const readlineSync = require('readline-sync');
const calculateBalance = () => {
    const incomes = readlineSync.question("Введите доход: ");
    const expenses = readlineSync.question("Введите расход: ");
    
};
console.log(calculateBalance());